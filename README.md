# Steganography App – Hide Secret Messages Inside Images

A Python GUI application built with Tkinter and Pillow to encode and decode hidden text messages in images using the Least Significant Bit (LSB) technique.

## Features
- Encode & decode messages in images
- Optional password-based XOR encryption
- Supports PNG, JPG, JPEG, BMP formats
- User-friendly interface built with Tkinter

## Requirements
- Python 3.x
- Pillow library (`pip install Pillow`)

## Usage
Run the program:
```bash
python steganography_app.py
